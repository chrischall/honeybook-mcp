# No required env vars in v2. Sessions are captured via the `use_magic_link` tool.
# Optional: pin the API version instead of auto-fetching from /api/gon.
# HONEYBOOK_API_VERSION=2578
# Optional: show the Puppeteer window during session capture (debugging).
# HONEYBOOK_SHOW_BROWSER=1
